import 'package:flutter/material.dart';

class AcceptPage extends StatelessWidget {
  const AcceptPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('접수 관리'),
      ),
    );
  }
}
